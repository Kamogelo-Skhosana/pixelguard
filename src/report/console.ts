/**
 * Console output formatter for raw diff results (Phase 1).
 *
 * Ticket: P016
 */

import type { DiffResult } from "../diff/models.js";

export function printDiffResults(results: DiffResult[]): void {
  // TODO (P016): format as a simple table (page, viewport, percent changed).
  throw new Error("Not implemented");
}
